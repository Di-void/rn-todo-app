import { View, Text } from "react-native";
import Home from "../screens/Home";

const Tabs = () => {
  return (
    <View style={{ flex: 0.7 }}>
      <Home />
    </View>
  );
};

export default Tabs;
