import React from "react";
import { View, Text } from "react-native";
import Input from "../components/Input";

const Home = () => {
  return (
    <View className="p-4">
      <Input />
    </View>
  );
};

export default Home;
